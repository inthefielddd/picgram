extends layouts/main
include mixins/imageBlock

block content
    .images
        each image in images
            +imageBlock({
                id:image.id,
                imgFile:image.fileUrl,
                title:image.title,
                views:image.views
            })